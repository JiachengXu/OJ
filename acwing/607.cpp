#include <algorithm>
#include <cstring>
#include <iostream>

using namespace std;

int main() {
  double a, b, c;
  cin >> a >> b >> c;
  printf("MEDIA = %.1lf", a * 0.2 + b * 0.3 + c * 0.5);
  return 0;
}